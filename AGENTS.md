# ai-trade 项目推进约束

## 当前持续授权（2026-09-22 用户更新，优先于旧阶段发布限制）

- 项目仍处开发/验证阶段。用户明确允许项目代码及必要工程文档直接合入现有公开 `skw2026/ai-trade` 的 `main` 并 push，通过现有 CI/CD 部署到现有测试环境验证；不再为逐次提交、主线推送或这类部署重复索权，不要求先建审批分支或新账户。
- 默认连续完成实现、范围内验证、提交、推送、精确 SHA 的 CI/CD 与部署后检查。失败暂停与根因/路线复盘仍有效；不得跳过门禁、伪造 PASS 或因“赶进度”盲重跑。
- 用户在单分段诊断入口方案后再次要求“不用每一步都让我确认”：BLOCKED 不阻断有界、只读、诊断专用工作流的代码发布与执行，不再为此单独索权。此类提交须隔离自动 CI/CD（例如 `[skip ci]` 并核对实际运行），不部署/重启交易服务、不改历史原件、不执行正式 gate retry、不解除 BLOCKED/HALTED；取证成功不能改写原验收失败。
- 用户随后在“固定工程回归与持续数据质量分别验收、历史 FAIL 保留”的建议后回复“继续”：本轮按 `docs/plans/2026-09-22-engineering-data-boundary.md` 实施明确的新边界。旧耦合验收以失败停止，不改旧 checker 或追溯 PASS；新独立工程阶段可按既有 main/CI/CD 授权交付。动态数据仍真实 FAIL/INSUFFICIENT、无研究/交易资格；未知技术失败和证据缺失仍暂停依赖步骤。
- 此授权不把工程通过变成盈利资格，不自动解除关闭候选、研究合同与风险限制；不包含实盘切换、资金划转、修改账户权限、公开凭据/原始账户资料或把本地历史证据包公开上传。
- 优先产出可裁决的结果：分别报告工程交付是否通过、策略方向是否获得经济证据；不要用分支/账户管理或反复文档步骤替代目标验证。只有新的实质产品/风险决策、缺失外部权限/信息或必要时间验证才形成用户停点。

## 开始与阶段边界

- 2026-09-23 用户明确选择“独立安全撤回”：按
  `docs/plans/2026-09-23-independent-safety-withdrawal.md`实现新风险语义并连续验证交付。
  旧严格批次及更早 HALTED 不改；新 gate 仅服务本合同。原始证据只读、工作副本
  隔离可写，线上 profile 和市场候选不变，不以部署代码冒称已启用或盈利资格。

- 2026-09-23 用户接续具体自学习验收方案要求“那就不要停，继续啊”：按
  `docs/plans/2026-09-23-strict-learning-integration.md`连续完成严格模型组合验证、
  仓位归因对照和限定修复。新独立工程gate，不重置旧HALTED，不改线上策略
  配置或重开市场研究；结构性严格回退缺口不可用普通模式回滚替代。
  本批已停止为HALTED：严格步长局部修复/99次实际模型评估及加仓归因完成，
  本机117/117；Linux全量两次分别遇到容量夹具和只读源码锁依赖，第二次
  53通过/1失败/63未执行。两轮修复已用完，不执行第三次或更换state继续。
  保留本地代码/失败，未推送部署；待严格退化安全语义的实质选择，接续须
  同时冻结隔离可写验证环境，不把本批改成PASS。见对应结果与停止复盘。

- 2026-09-23 用户以ok批准追加证据传输纠正批次，按
  `docs/plans/2026-09-23-evidence-transport-correction.md`连续落地，最多2有效
  工程小时/1次纠正代码发布。旧三个HALTED及缺件证据不改，使用该合同绑定
  的独立新gate；不重新索取普通提交/部署授权，不扩大研究或交易权限。
  本批已由25e0116一次发布及完整收据核对交付，115/115、88份文件完整，
  新gate READY，旧三个HALTED未改。详见对应纠正结果报告；不因下列历史
  待预算/失败记录自动重复修复或重开研究，当前市场候选资格仍未通过。

- 2026-09-23 注册隔离批次最终交付失败：e219065已部署、114项回归通过，但
  上传包排除两份隐藏锁文件。两轮修复额度耗尽，原离线工程gate已按
  `docs/reviews/2026-09-23-registration-artifact-upload-review.json`停止为HALTED。
  缺件ZIP/清单/失败不改，不补造空文件、不删断言、不自动第三次修复或换state。
  当前只归档脱敏诊断；拟议最多2有效工程小时/1次纠正发布的证据传输批次，
  需用户预算决定后才能接续，不重开市场研究或实际候选。

- 2026-09-23 用户接续“注册/晋升链隔离验收”回复ok：按
  `docs/plans/2026-09-23-registration-isolation.md`连续完成强制回归覆盖、真实训练
  产物的隔离拒绝/身份/回滚验证及交付。沿用READY工程gate，不写生产注册表，
  不运行真实canary，不重开研究或改变资格门槛。

- 2026-09-23 用户要求自动执行已确定的后续计划：本批按
  `docs/plans/2026-09-23-evolution-clocks.md`完成评估/更新时钟解耦及交付。
  沿用READY的离线工程gate历史，旧HALTED及已结案产物不改；不变更当前策略配置。

- 2026-09-23 用户以“ok”批准纠正 CD 观察命令固定旧 run 的流程缺陷：
  同一离线阶段 gate 经确认根因复盘后，可一次绑定同仓库 main/CD 的纠正 SHA/run，
  保留原失败、原命令 hash 和验收标准；使用 typed `release-prepare` / `release-retry`，
  不允许任意换 argv、重置 state 或解除旧 HALTED。仅本次门禁实现的隔离单测可先行自举，
  随后正式完整回归仍经原 gate；成功后按既有授权完成一次纠正发布及部署后验收。

- 2026-09-22 用户批准按目标对齐方案自动落地：本批执行
  `docs/plans/2026-09-22-offline-learning-loop.md` 的 16 有效工程小时离线验收。
  使用独立 `.artifacts/offline-learning-loop-20260922/validation-state.json`；
  旧研究/旧工程 HALTED 不重置。不涉及新市场研究、真实候选激活或账户交易。

- 先阅读 `docs/CURRENT_STATE.md` 和本次任务涉及的最新阶段报告；历史计划不得覆盖当前结论。
- 在用户已确定的目标、范围和预算内默认连续推进，不因内部步骤完成、文件交付或正常阶段切换反复请求“批准下一步”。不自动扩大为未授权的下单、资金/账户操作、ECS 发布、新研究方向或已关闭路线重开。
- 每一步开始明确目标、通过条件和失败出口；结束后报告结果、证据范围及下一步计划。

## 连续推进与人工停点

- 用户于 2026-09-21 明确要求减少逐步授权：普通实现、公开资料核查、范围内的数据处理、测试、问题诊断、限定修复与文档收尾由执行者连续完成；进度汇报不是授权请求，不把自己拆出的子步骤制造成新的批准点。已获明确授权的动作不重复索权；用户明确限定“只评审/不回测”等边界仍有效。
- **需要用户实质决策才交还：** 例如改变目标/产品范围/验收口径，超过约定成本或预算，重开已关闭路线，新增高影响外部操作，或缺少只能由用户提供的信息、凭据或平台权限。先穷尽安全的范围内诊断；一次说明事实、可选方案、推荐项和选择后可连续完成的边界，不只问“是否继续”。不能用“减少确认”绕过平台权限或交易/部署边界。
- **必须经过时间验证才进入等待：** 写清等待原因、最早有效复核时刻/事件、通过与失败条件，使用可用的等待/监测机制；条件具备后在原范围内接续，不再索取同一授权。没有配置实际监测时不得承诺后台自动运行。普通构建、测试、网络冷却和可自行完成的诊断不默认交给用户等待。
- 验证失败先暂停依赖步骤，不等于立即结束工作向用户索权。自行完成根因分析、路线复核、原范围内限定修复及门禁允许的一次复验；仅当处理触及实质决策/外部依赖时才交还。HALTED、已关闭合同和预算上限不能自行重置。
- 达到目标或预定否定出口时明确交付结案；这是完成，不是再请求一次“执行下一步”。不存在已授权且有依据的后续工作时，不为保持忙碌自动换策略、续预算或安排等待。

## 验证失败必须暂停

- 开展实现或验收前完整阅读 `docs/VALIDATION_PROTOCOL.md`，执行 `python3 tools/validation_gate.py status`。
- 正式 configure/build/test/验收命令通过 `tools/validation_gate.py run` 执行；不用裸命令、换状态文件或删除记录规避阻断。
- 任何未预期的验证失败、必需证据不足、超时或 CI 失败，先暂停所有依赖步骤。可以继续只读诊断、明确标注的复现和有依据的限定修复，不能继续长跑、堆样本或交易服务发布；上文隔离的诊断专用入口不属于依赖失败结果的发布。
- 每次失败都必须分析根因和实现路线：目标对齐、数据可得、方法可达、预算与停止条件。未知必须标 unknown，不能编造原因或降低验收标准。
- 连续两次未解决失败，或首次结构性不可达，必须重审整条路线，不只修局部症状。涉及改目标、改验收或扩大权限，先取得用户决定。
- 复盘经门禁接受后只允许一次原命令 `retry`，成功才恢复依赖推进。若决定停止，明确关闭当前路径，不自动换策略或重开预算。
- 远端 CI 失败同样适用，不能靠直接 rerun 绕过；API 请求成功、采集成功和统计资格通过是不同结果。

## 证据与交付

- 不删除或覆盖历史失败证据；重要复盘须随阶段报告归档。不得公开凭据、账户原文或账户金额。
- 不把旧运行快照当成新观测，不把分支 CI 当成合并提交自身的 CI，不把工程通过当成盈利资格。
- 当前候选关闭锁存、`NO_QUALIFIED_CANDIDATE` 和 C2 `NOT_QUALIFIED` 不因工程修改自动解除。
- 未获 ECS 发布授权的主线交付必须避免自动 CD，并在推送后验证实际工作流和部署记录。
