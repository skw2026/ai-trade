# 严格自学习组合验收（事前范围与裁决）

2026-09-23 用户在具体接续方案后要求“那就不要停，继续啊”。本批连续完成
代码核查、隔离组合验收、归因对照、必要限定修复及符合门禁的交付，不逐步索权。
自设投入上限4有效工程小时、最多两轮限定修复、一次常规代码发布；非自动续用
旧16小时/2小时预算。若出现结构性不可达，立即复核整条路径，不消费剩余时间
凑通过。独立工程状态 `.artifacts/strict-learning-20260923/validation-state.json`；
此前三个HALTED及已结案证据保持。没有新市场研究、数据下载、账户操作或候选激活。

## 固定剩余清单（不能用局部 PASS 覆盖）

| 缺口 | 类型 | 本批交付/边界 |
| --- | --- | --- |
| 真实模型到严格反事实时间留出选择 | 组合证据缺失 | 同一实际模型/原合成数据，逐笔独立重算选择、留出和权重更新 |
| 自适应总收益含加仓影响 | 归因缺失 | 配对同时间/同方向交易，以固定名义本金重算；可以得出无独立增益 |
| 严格更新步长与恶化回退 | 行为待证 | 用固定输入暴露缺陷；既有步长约束可限定修复，禁止偷偷放开严格回滚旁路 |
| 生产调度/注册/真实canary连续证据 | 尚未全链证明 | 保留；本批不以合成注册通过代替候选经济资格 |
| 线上小时模式样本可达性 | 配置前置未验收 | 保留；不改现有线上profile，短窗口不足必须冻结 |
| 真实市场候选与账户效果 | 经济/外部证据缺失 | 保留关闭结论；不重开历史失败路线、不安排默认两周等待 |

## 固定输入与通过条件

- 复用已交付的实际CatBoost模型、报告与合成OHLCV，绑定hash；不重新训练、
  不换随机种子、不调参搜索。CD若进入发布，仍执行原固定真实学习生成器。
- 新测试只运行生产模型推理/政策和SelfEvolutionController，无交换所接口。
  信号至少延迟一根bar送入虚拟归因，不用未来收益或生成器cue代替模型方向。
- 新严格诊断使用240根5m条的固定窗口、70%选择/30%时间留出，保留默认最少
  10/10样本，开启learnability（120,1.5）和留出优势门槛（10,1.5）。
  240由样本容量事前确定，不是线上小时配置或收益调参；12条短窗口作不足负例。
- 独立Python重算训练段最优权重、留出收益/统计量及每次更新的上限/步长；
  任何更新必须对应实际验过的候选，不允许评完大步候选后仅裁剪成另一候选。
- 修改后段结果不得改变同一窗口前段所选候选；后段不利必须拒绝更新，不走
  factor-IC/全窗口旁路。真实推理需绑定同一模型和训练期，不伪造governance。
- 归因对照使用旧positive/adaptive同一决策时间和方向的完整episode，明确
  名义本金统一仅消除仓位缩放，不等于完整风险归一化或真实市场alpha。
- 普通模式原有回滚/冷却测试保持，但不得声称它证明严格模式回滚。
  严格回退若不可达，组合目标必须报告 NOT_COMPLETE，不能删断言或改为全通过。

## 事前源码发现与失败出口

正式执行前源码核查发现：候选网格以max_weight_step为间隔但选优未限定相对
当前权重的距离；严格holdout路径明确禁止ShouldRollback分支。前者需实际
复现后按既有单步上限限定修复；后者必须区分统计候选替换和安全撤回。
不得直接删除严格保护条件、把未经留出验过的基线当成新合格候选。

全部正式构建/测试经本批gate。失败先停依赖，原输入保留，根因与目标/数据/
方法/预算复盘后一次原命令复验。若安全回退需要修改既有严格契约，完成安全的
独立核查并给出具名设计选择；不部署不满足本批目标的交易代码。
通过才接现有完整回归、main/CI/CD和同SHA独立收据验收；结案允许否定/未完成，
不以保持忙碌为由续预算或重开研究。
