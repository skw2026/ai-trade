# 原 MVP 最小合同改造（批准后执行）

开始：2026-09-21 11:52:02 UTC；最多 4 个有效工程小时，连续工作以墙钟为保守上界。用户批准上一轮限定改造。起点 main `9b76b3daffc433bfc1a4f2f764415d51a24fa98d`，保留上一轮所有未提交诊断/证据，不重写其结论或哈希。

## 权限和验收

仅本地源码、合成测试、构建和报告。不调参、训练、下载历史行情、访问账户/凭据、采购、提交/推送、部署或交易。工程验收后评估历史筛查前置条件，不自动把本轮扩大为历史实验。旧候选关闭、C2 NOT_QUALIFIED 不变。

1. 风险生命周期：已向用户询问人工授权新周期/永久锁存/自动恢复的选择。没有答复前保留原锁存，不自主复位回撤。所有新周期必须保留累计损失和全生命周期回撤，不能伪装成风险通过。
2. 信号时钟：新增显式 `system.closed_bar_mvp` 离线模式，固定 300000ms；同一组已收盘 OHLC 在不同 tick 切分下给出同一基础趋势和 regime。未闭合 K 线不得生成新策略决策，行情仍可触发风险减仓。旧模式保持默认 false。
3. 成交时钟：新模式将输入 K 线拆为因果 open/close 事件。普通 taker 只在后续同 symbol 的 open 事件成交，以届时价格而非旧订单参考价计价；不偷看该 bar 的高低收盘。资金费在边界对原持仓计一次；末端平仓单独标记、计成本并撤销无后继的入场单。
4. 验收：合成用例覆盖未闭合/跨 bar/多 symbol/不同切分/未来价格扰动/资金费/取消/末端结算/风险恢复。先定向，后全量回归；失败立即暂停，分析后只允许一次原命令复验。合成通过不等于经济通过或 Demo 权限。

为保持既有研究身份，新增模式不默认开启，不修改现有运行 YAML，不修改上一轮审计工具。新配置只冻结原 MVP 组件及既有基础参数，不做收益选择；不允许 phase2 模型/防御/自适应路径混入。

## 执行进展

**后续完成：** 用户确认仅人工批准新风险周期后，已按[固定人工规则](2026-09-21-mvp-manual-risk-cycle.md)落地，2026-09-21 13:37:27 UTC 全量 **100/100 PASS**。三个行为合同的本地离线工程验收完成，原风险选择阻断解除；历史经济筛查仍未启动，不自动恢复旧额度。见[最终工程结案](../reviews/2026-09-21-mvp-manual-risk-cycle-result.md)。下段保留确认前的阶段事实，不再是待办。

2026-09-21 12:11:57 UTC，全量回归 99/99 通过（158.22 秒），步骤 2/3 的时钟及成交合成验收通过。两次实现错误均先暂停、记录根因后一次原命令复验通过，没有扩大为历史实验。

步骤 1 的风险恢复选择尚未收到答复；保留原锁存，未实现新周期复位。步骤 4 因此只有工程回归通过，**整个合同尚未通过，不启动历史筛查**。详见[阶段结果和剩余产品决定](../reviews/2026-09-21-mvp-contract-repair.md)。后续在同一 4h 上限内续接所选风险合同及合成验收，不重开预算、不换 alpha 路线。
