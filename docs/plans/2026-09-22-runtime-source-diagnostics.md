# 封版后运行源诊断：有界工程接续

用户在工程结案后要求继续推进。只接续已有 Smoke 两项告警的原因诊断，不重开研究、不生成新候选。

固定输入：部署 `e47e21b` 的 Smoke `35682446670` 已下载原始日志/报告、当前源码和合成测试。远端 Actions 只读检查没有新部署；现有日志是 03:13–03:16 UTC 窗口，不冒充此后的实时 ECS 观测。

已确认：旧 integrator 初始化被治理门槛拒绝，缺训练标的、bar 周期、来源及成本后目标字段；`microstructure_demo.enabled` 使启动代码保留 canary 模式并记录 router armed。因此 mode=canary 不证明模型可用。微观结构源首次未通过生命周期检查时静默返回 unavailable；仅凭没有 accepted 事件，不能判断它是文件缺失、候选未准入还是其他完整性问题，精确原因仍 unknown。

限定改动：在已有 runtime JSON/控制台及闭环摘要中附加 `integrator_availability`，只输出枚举/白名单原因码，按最新观测 boot 重置。明确 UNKNOWN、初始化/治理拒绝、仅 router armed、信号接收/失败关闭等事实；不把日志诊断变成交易或经济资格。不删除现有两项警告，不改 verdict/门槛，不改 C++ 下单、配置、账户或候选。

通过条件：合成缺失/拒绝/恢复/跨 boot/信号状态/脱敏/旧报告兼容测试通过；真实旧日志离线复算准确解释拒绝且原验收字段不变；main 精确 SHA CI、CD 和三项 post-CD 实际成功，新 Smoke JSON 及闭环摘要都含诊断并保持研究关闭。历史报告原件不覆盖，不上传日志/账户数据。

预算：03:34 UTC 起本地诊断/实现/验证最多 30 分钟、只发布一批，远端按原工作流超时观察。任一未预期失败暂停依赖步骤，使用现有工程 gate 复盘；旧研究 gate 保持 HALTED。工程出口是诊断可靠交付，不是消除资格阻断；完成即停止该修复，不追加训练/行情实验/两周等待。
