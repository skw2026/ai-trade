# main 直接交付与 CI/CD 部署验证

用户于 2026-09-22 明确扩大持续授权：现有公开项目可以直接 main 合入、push 并通过 CI/CD 部署验证，不需要逐次索权，分支/账户治理后置。本计划取代上一轮独立分支/不部署的待授权安排，保留原证据和失败暂停协议。

## 本轮只回答两个问题

1. 已封版工程能否通过同一主线提交的 Linux CI、Docker 全量回归、镜像发布、现有 Bybit Demo 环境部署与部署后检查？以这些具体阶段的真实结果为准，不把 skipped/pending 当 PASS。
2. 这些结果能否证明策略经济方向？不能单凭工程证明。已关闭研究的否定/证据不足结论保持，当前部署不启用离线 MVP 配置、不改策略/风控参数；最终报告明确工程与经济结论，防止继续漂移。

## 执行范围与节奏

- 输入是封版 `a1079a5`、预检文档 `69bfdf5` 及本轮最小发布保护：Docker 内部三步也通过失败门禁，本地 `.artifacts/` 和运行凭据排除出 Docker 上下文，两个静态回归接入现有 CTest 项。
- 先通过本地定向与全量验收、冻结实现/归档身份校验；现有独立工程 gate 必须 READY，旧研究 gate 仍 HALTED。
- 直接 main 快进推送。旧两个 `[skip ci]` 提交先作为已验收锚点送达；随后推送本轮无 skip 标记的交付提交，保证新一次 CI/CD 针对明确 SHA。无强推、无 research 标签、无新策略试验、无原始证据包上传。
- CI 失败阻断镜像发布；CD 既有 exact-CI、镜像 digest、主机指纹、启动检查、release 身份、健康门禁与失败回滚保持，不降低阈值。未改变现有 Demo 账户或服务配置。
- 跟踪同一 SHA 的 CI、CD 与既有 post-CD 检查，公开 API 只读观察；不在发布中途推进另一批 main 提交扰动身份。若需要修复，先保存日志/根因/路线复核，再有界修复，不无分析 rerun。

## 成本与停点

23:50:12 UTC 开始（北京时间 9 月 22 日 07:50:12）。本地准备和回归限 30 分钟；本轮仅一个发布批次。CI/CD 与部署后任务按仓库自身超时预算观察（CD 部署 job 180 分钟，包括既有最长 90 分钟事务锁等待），不将正常异步等待称失败或另索同一授权。

发生首次非预期失败即暂停依赖推进并定位最早失效环节；一次有根因的范围内修复/复验，第二次未解决必须重审路线。缺少只能由用户提供的凭据/平台权限、需要改产品风险口径、或需要扩大实盘/资金权限才交还。无等待两周、采购数据或把旧合同重新计次。

## 交付出口

提交 SHA、CI/CD run ID、真实 job/step 结论、部署与 post-CD 身份，以及本地完整测试、修复和原门禁身份归档。若部署失败/回滚/外部阻断，明确停在哪一步，不称完成；通过则关闭此次工程部署阶段，并明确盈利方向未由该验收证明。结果文档可本地留档或之后 docs-only `[skip ci]` 归档，避免为记录结论再部署一次。
