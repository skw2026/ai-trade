# STOP 后实施：关闭治理 → 真实账务 → 经济归因

依据 [9 月 12 日圆桌裁决](../reviews/2026-09-12-option-lifecycle-v4-stop-roundtable.md)，用户要求按顺序实施，直到取得合格下一候选并进入明确的等待验证阶段，或遇到必须补充的证据/授权。不得承诺能通过优化产生盈利候选。

## 当前实施范围

- C1：新增独立关闭注册表及审计器；固定首次 STOP 的 GitHub run、artifact、ZIP SHA256、release 和经济合同。GitHub Actions 通过自身只读权限下载原产物，验证三份报告及六期会计/聚合恒等式。本地 14 项新测试、77/77 CTest 完整回归、warnings-as-errors 构建及 YAML/Bash 语法检查通过。
- 注册表按经济合同绑定已关闭候选，不能通过改候选名、改注册表或最新 batch PASS 自动重新晋级。未知/损坏证据 fail closed；冻结 payoff/economic 源码和四个合同保持不变。
- 治理报告与原 ZIP 一同进入研究产物，有限长度、白名单字段的 annotations 提供可核对的研究数值与身份，不包含原始行情、账户或凭据。
- 关闭治理进入研究工作流，不接入发单或账户操作。当前采集继续保留；C1 不代表 C2–C5 已通过。

## 结果出口

| 阶段 | 当前状态 | 必须取得的结果 |
|---|---|---|
| C1 关闭证据和锁存 | 已通过：`4a6d581` 的 CI/CD、V4 Gate、Archive Audit、Smoke；真实 ZIP 和六期核验成功 | 原始 anchor 保留，输出 `CLOSED_CANDIDATE_NO_REOPEN`；见下方结果报告 |
| C2 一期真实账务 | 未通过；已留存本窗口 18 条结算费率，全段/分段一致 | 仍需确切结算价格、因果持仓、适用费用与保证金模型证据，才能接通完整账务；不能用 candle/预测值冒充结算真值 |
| C3 六期归因 | 已完成 C1 所需的冻结费用分解；完整资金账归因未通过 | gross 合计正、对冲费用是主要已列成本；补齐 C2 后再检查真实资金费与资本风险，不覆盖旧 STOP |
| C4 新候选资格 | 未开始 | 新经济假设与未见合格历史支持，经过资本和成本淘汰；不以换名/改阈值重开旧候选 |
| C5 新冻结前向 | 未开始 | 前置门全部满足，并注册新样本边界、统计检查点和停止规则后，才进入等待 |

## C1 证据留存边界

原始 anchor artifact 于 2026-12-11 到期。本轮工作流将其经校验的 ZIP 复制进每次新产物，但 GitHub artifact 不等于永久存储。归档取得后需形成长期保留副本与身份索引；不可在原证据过期后无条件把新 latest 当成同一个 anchor。获取失败时工作流拒绝出具验证通过结果，注册表关闭状态不自动解除。

本地合成测试证明规则，不构成真实收益证据。部署成功和下一阶段是否盈利分别报告。现有用户未提交的三个文件继续保留，不混入本次实现提交。

本轮真实关闭证据、六期数值、首期执行价勘误和 C2 数据缺项见 [C1 实施结果](../reviews/2026-09-12-option-candidate-closure-result.md)。没有注册新候选或启动新的盈利验证时钟；当前不能称为“全部问题已解决、只需等待”。
