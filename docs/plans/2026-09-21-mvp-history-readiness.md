# MVP 历史输入准入核查（不运行历史筛查）

开始：2026-09-21 13:56:58 UTC。用户要求执行下一步；本步承接工程结案所列输入/资本/成本绑定，不恢复已关闭的历史额度。最多 1 个连续工程小时，0 次行情 GET、0 次历史经济回放；只查官方文档、本地公开研究档案与合成诊断。不访问账户/凭据，不提交、推送、部署、交易或改策略参数。

## 目标、已发现问题与停止边界

目标是回答“同一 MVP 能否进入一次有效历史筛查”，不是再证明单元测试通过。准入需要同时具备唯一实现/配置、合格输入、资本/保证金语义、成本、裁决与停止合同。

源码核查发现 replay 持仓的 `liquidation_price` 恒为 0；本地账本新持仓也不计算强平价。风险引擎将其视为 unknown，禁止加仓。已暂停依赖的行情下载、转换器实现和经济筛查。下面仅做预先声明的合成定位，不能以诊断命令返回 0 宣布准入通过。

## 限定合成复现

- 新增显式构建的诊断目标，不改交易实现，也不加入默认测试代表经济资格。
- 固定现有 `config/bybit.replay.mvp.yaml`；3 根恒价 5m trade/mark、funding 为 0 的人工 fixture，分别观察多头和空头，不使用历史行情。
- 真实 replay adapter 下一开盘成交后，观察账本与 adapter 的强平信息；额外刷新一次本地模拟风险快照，检查是否补齐。
- 用原 RiskEngine/ExecutionEngine 隔离观察：空仓开仓、持仓后加仓、原仓持有、部分减仓、反向目标；用 TradeSystem 检查 unknown 原因码。
- 预期诊断确认：空仓能开，持仓信息未知，加仓被禁止，持有不必平仓，减仓/只平旧仓仍可；已知安全距离仅作为独立合成对照，不写回正式状态。
- configure/build 上限各 120 秒，诊断 30 秒，定向回归 120 秒；任何非预期失败按门禁协议暂停/复盘，不换命令掩盖。

## 交付与出口

归档真实诊断输出、文件哈希、本地数据可复用性及完整准入矩阵。已知能力不足则输出 `INSUFFICIENT_REPLAY_CAPITAL_CONTRACT`，停止当前“补行情就能历史筛查”的路径；不能关闭 unknown 保护、凭空填入强平价，或将当前阉割行为收益作为正常趋势/VolTarget 的否定。

改变参考保证金模型属于经济解释的实质选择，需明确提出一次有界修复方案并取得决定；官方公开市场资料不能重建一个假想持仓的账户强平价。旧 100/100 行为工程结案、研究关闭和 C2 未通过保持，不改写旧证据。
