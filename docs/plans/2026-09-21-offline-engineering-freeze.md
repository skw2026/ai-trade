# 离线工程封版：范围与验收

授权：用户明确选择“先完成工程封版”，承接上一轮提出的改动审查、测试接入、独立工程验收、数据归档与本地提交。此阶段不以盈利或 Demo 准入为验收目标。

开始：2026-09-21 15:50:43 UTC；主动设置 2 小时执行上限，17:50:43 UTC 到限交付已完成项及阻断，不自动续预算。

## 交付范围

1. 审查当前未提交的闭合 5m MVP、人工风险周期、显式离线参考账务、输入/裁决/采集工具及阶段证据；保留用户改动，不加入无关文件。
2. 将公开采集器的纯合成测试接入 CTest，补充只读停止账务诊断的合成回归。统一测试不能访问网络、账户或运行已关闭历史策略。
3. 在全新构建目录完成 configure/build、定向及全量 CTest；固定研究合同、原配置与源历史证据不改写。普通缺陷按失败协议处理；需要改变金融含义/验收时停止并交由用户决策。
4. 本地归档已完成阶段的公开输入、合成运行记录、失败证据及依赖的旧二进制；生成清单和 SHA256，逐文件核对归档。原件不删除，归档不上传、不进入 Git；同机副本不是异地备份。
5. 写明离线入口、已关闭入口、恢复归档方法及测试证据身份；审查暂存差异后本地提交，使用 `[skip ci]` 标记。不推送、不部署、不重启服务，不触碰账户。

## 独立门禁，不重开历史研究

- 新工程门禁：`.artifacts/engineering-freeze-20260921/validation-state.json`，仅用于这次用户明确选择的独立工程交付。
- 新构建目录：`.artifacts/engineering-freeze-20260921/build`。保留原 `build/`，避免覆盖历史运行依赖的二进制。
- 原门禁 `.artifacts/validation-gate/state.json` 保持 HALTED，failure_id `a16b1aed424847bcb25e7ade63536cb5`；原状态 SHA256 `bc576299059d630fcf400de46dc16b38d6c83591c1dc193433a9c32322f6910b`。
- 所有正式验收经新门禁；任何失败先停止依赖工作、确认根因与路线，再一次原命令复验。不因工程新阶段恢复旧历史采集、base/stress、预算或候选。

## 通过条件与出口

- 最终提交内容与通过测试的源码/配置身份一致；正式 configure/build、定向与全量测试均通过，失败记录保留。
- 采集及诊断新测试只使用合成输入；不存在新历史实验、网络请求、账户访问或交易。
- 归档能逐文件读取并与清单校验，旧研究证据及原失败门禁未变；数据位置、大小和 SHA 可追溯。
- 工程封版提交在本地可定位，工作区内本阶段改动收齐；若发现无关用户改动则排除并明确报告，不强制清空工作区。
- 结论只能是 `OFFLINE_ENGINEERING_FREEZE_COMPLETE` 或列明未完成项的阻断；不得由工程通过推出盈利、C2 或 Demo 资格通过。

终点为本地工程交付；提交推送/远端备份/部署和新研究方向都不是本阶段动作。
