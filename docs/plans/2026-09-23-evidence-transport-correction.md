# 证据传输纠正批次（事前合同）

用户在“最多2小时、1次纠正发布”提案后回复ok。2026-09-23 12:00 UTC起，
连续完成本次纠正；不把上一批失败改成PASS，不重开研究或真实候选。

## 边界与固定输入

- 旧release为e219065d7d5e9950177d804b775ad4f5f6701898，失败ID为
  747c331083a74e6a99e405b617a14b25；旧缺件ZIP及原清单不修改。
- 旧离线工程gate保持HALTED，sha256为
  a1afc2526b05335c2c8f6c408c5c12aea5081dc5790a1002bc8e779525a04116；
  旧研究/旧工程两个HALTED也保持原hash。使用用户明确批准的独立纠正批次
  `.artifacts/evidence-transport-20260923/validation-state.json`，不提供reset。
- 复用原真实学习/注册隔离固定输入和验收标准；本地只复制现有TEST_ONLY
  产物做传输测试，不下载行情、不训练。CD仍执行原六组固定学习和注册隔离。
- 不改src/config/模型参数/资格门槛/账户权限，不执行真实注册、canary或回滚。

## 唯一修复路径

1. 对专用合成学习目录做上传前清单：验证学习/注册报告的TEST_ONLY来源、
   同一SHA、报告及全部文件hash、两个锁文件存在、目录中无额外文件或链接。
   所有相对路径必须安全；清单放在独立兄弟目录，不混入递归hash。
2. 仅此专用目录显式include-hidden-files:true，不扩大上传根。
   上传前检查未成功，不上传未经检查的目录。原失败日志仍保留在Actions。
3. 按本次upload返回的唯一artifact ID下载到新目录，再按上传前清单逐项比对，
   缺件、损坏、越界、额外文件、身份错配均失败。部署依赖回读核验及收据上传
   成功；清单和回读结果另存专用artifact，避免替换原学习/注册报告。
4. 新回归纳入CTest/CI，覆盖以上负例、隐藏锁文件正例、真实CLI和CD依赖顺序。
   完整旧114项保留；至少115项。旧坏ZIP作为必须拒绝的固定负例，不补造文件。

## 验收和停止

本地定向/完整回归、Linux隔离传输测试、旧坏ZIP拒绝、完整真实本地产物往返
均须通过后，最多一次main纠正代码发布。精确SHA的CI/CD（含上传后真实下载
核验）、Smoke、固定回归和原工程/数据拆分合同全部核对；旧V4唯一坏段不改。
最终下载的新包仍须通过上一批完整注册清单和学习/时钟/部署验收，加上新回读
收据身份与三份旧HALTED hash核对。不能以部署前检查代替最终收据验证。

最多2有效工程小时、1次纠正代码发布；新正式验证失败立即停依赖、完成只读
根因/路线复核后按失败出口结案，不自动增加纠正轮次或重跑工作流。普通构建/
观察用等待机制，不增加人为两周等待。文档结案可单独[skip ci]归档并核对未部署。
