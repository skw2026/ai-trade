# N1 ETF 历史可知时点：一次限定核查

状态：**COMPLETE / INSUFFICIENT_POINT_IN_TIME_EVIDENCE**。05:25:16 UTC 停止外部查阅；此后只归档和验收文档。维持 `NO_GO_NEW_ROUTE`，不申请回测、不自动续查。结果见[时点核查报告](../reviews/2026-09-21-etf-timing-audit.md)。以下保留查阅前固定的标准。

用户选择上一轮选项 2：只核查已找到来源的历史发布时间、修订记录，最多 30 分钟，不回测、不采购；无证据即结束。选项 1 的提交/推送未获本次选择授权。

开始：2026-09-21 05:21:13 UTC；硬截止：05:51:13 UTC，含查阅、报告及文档验收。本文件在本次外部查阅前记录标准。不扩展为第四条路线，不重置原三路线评审的预算或结论。

## 固定目标、来源与通过标准

只判断 N1 的历史信息可得性是否存在可审计的现成路径，不判断盈利、不执行实验。

- 来源限于上一轮作者 `boonchuan/etf-price-impact` 及相关作者仓库的 README/公开 Git 元数据、Farside 自身发布/修订说明与官方存档索引、iShares IBIT 官方披露说明。允许检索这些来源的时点文档，不转去寻找新供应商或新策略。
- Git API 只取仓库、提交列表、树和 blob 身份等元数据；不打开 CSV 内容或包含数值的提交补丁，不下载行情样本，不运行作者代码。
- 可通过的证据是：提供方可追溯的历史发布与修订版本规则/入口及适用覆盖，或能在决策前证明已可见且绑定内容版本的现成历史快照。精确首次发布到秒不是必要条件：可信快照给出的可见时间上界也可用于未来合同，但收益窗口必须在该上界之后。
- 需要分清 `data_date`、`published/observed_at`、`revision/version` 和预测窗。仓库最终上传时间、网页当前抓取时间、`as of` 日期或无上界的“通常晚间更新”，单独均不足以证明旧样本当时可见。
- 只发现数据入口/普通版本史不能判为资格通过。若元数据足以支持后续有界核验，只提出申请；本次不读取样本来完成覆盖验收，也不默认进入回测。

## 预算与失败出口

查阅、必要只读诊断和结论共用 30 分钟。禁止采购、供应商联络、登录/账户访问、数据采集任务、策略实现、训练、交易、部署或推送。公开网页附带显示的市场摘要不抽取或计算。

缺证即结束本次核查，保持 `NO_GO_NEW_ROUTE`，不等待两周、不换供应商、不以任填 T+1/T+2 修复。读取失败先分析来源/工具限制；不盲重试，不把访问失败推断为数据不存在。正式文档验收走现有验证门禁，失败按协议处理。

## 交付

一份包含证据、时点区别、未解决项、根因/路线复核和下一步边界的本地报告；同步当前状态。最终只能是“存在可审计路径，另行申请核验”或“现有证据不足，本轮停止”，都不是盈利/历史资格 PASS。
