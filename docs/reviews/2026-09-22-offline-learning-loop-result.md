# 离线自学习闭环阶段结果

最终结案（2026-09-23 北京时间）：**OFFLINE_LEARNING_COMPONENT_LOOP_DELIVERED**。
纠正代码 `2a4f464146c0a21ca591b989a00da6966438d0ef` 已提交 main、推送并部署。
本地、远端CI、Docker内完整测试均110/110；Linux真实学习验收及产物校验通过，
Smoke和固定工程回归成功。原阶段gate READY，旧失败保留，旧研究/旧工程HALTED未改。
详细身份与脱敏数据见[机器证据](2026-09-23-offline-learning-delivery.evidence.json)。

原 `ea853a3` 发布的权限失败及固定旧run的入口错误没有追溯改绿：
用户批准后，经同一gate的根因/路线复盘、完整准备测试和一次纠正SHA/run绑定完成。
详见[原始阻断与根因](2026-09-22-offline-learning-publication-blocked.md)及
[限定复验记录](2026-09-23-offline-learning-publication-review.json)。
不是整个自学习产品、盈利资格、真实 canary 或生产晋升已通过。
本批依据[16小时冻结计划](../plans/2026-09-22-offline-learning-loop.md)，不重开旧研究。

## 真实学习证据（本地与远端均通过）

真实 C++ Miner → Python 生产特征/训练函数与 CatBoost → 同一序列化模型 →
C++ Integrator 推理 → 生产策略政策/风控/执行意图 → 合成成交账 →
生产自进化控制器反馈；不是 oracle 方向或手填经济通过报告。

- 正例：495 个不重叠、完整平仓 episode；独立成本后均值/95%下界为正。
- 固定模型/固定权重与自适应组采用相同预测及成本；后者实际调权 2 次，
  0.50→0.55→0.60。14 次更新决策不冒充 14 次实际权重改变。
- 噪声 491 episode、打乱标签 1 episode、高成本 495 episode 均不获得资格。
- 规律反转 742 episode，产生 2 次回滚、6 次冷却评估；上限、步长、最短
  更新间隔和冷却内禁止更新均由驱动逐次断言。
- 六组 Python/C++ 概率最大差不超过 1.12e-16（合同1e-5）。
- 六组独立逐笔重建的费用、资金费、净损益与 C++ 账匹配，终态空仓。
- 同一候选重复回放的 trace hash 相同。错身份、缺模型、缺 bar、过期输入、
  断连/未知风险/熔断新增仓位被拒绝；strict 接管始终拒绝合成报告。
- 本地完整 CTest **110/110，179.46 秒**；最终验收驱动单测 **8/8**；
  最终脚本在发布同等的断网/只读/无 capabilities/2GiB 容器中独立复现通过。

上述179.46秒是初次本地学习交付前记录。纠正发布前完整回归110/110、178.45秒，
门禁隔离测试22/22；远端CI110/110、227.42秒，Docker内110/110、152.22秒。
远端六组episode/调权/回滚计数与本地一致，Python/C++最大概率差1.12e-16以内；
模型/训练输入/trace等全部学习产物哈希及验收/训练源码哈希已核对。

本地原始证据：`.artifacts/offline-learning-loop-20260922/acceptance/`；
最终发布配置复核：同目录 `publication-contract/result.json`。
全部行情、模型、成交均为 TEST_ONLY，没有市场或账户经济证据。

## 两次真实失败，没有删证或改绿

1. 固定组错误读取 disabled 控制器未初始化的权重；修正为冻结常量0.5/0.5。
   [第一次复盘](2026-09-22-offline-learning-disabled-controller.json)。
2. 驱动把生产政策已带符号的金额再次乘方向，造成空头变多头。
   独立账识别错误；逐笔重算该错误行为与原 C++ 账差2.3e-13，根因已确认。
   完整路线复核后直接传递 signed notional，并增加方向一致性断言。
   [第二次及路线复盘](2026-09-22-offline-learning-signed-notional.json)。

两次修复都只改测试适配层，未改生产策略、模型超参、随机种子或验收阈值。
两次复验复用原训练模型，分别保留 retry-1 / retry-2；旧失败不覆盖。
这也暴露执行者首次接线检查不足，不能将其解释为新的市场问题。

## 自动化交付

- CTest 注册快速驱动回归，CI检查其存在；研究镜像单独携带离线驱动，
  交易 runtime 镜像不携带该二进制。
- CD 在构建研究镜像后，按不可变 digest 拉取并核对提交身份，强制执行
  `Verify Offline Learning Loop`；无网络、只读根文件系统、无账户凭据。
- 必须通过才继续部署，结果和合成证据自动归档；没有 continue-on-error。
- 本批远端实测已完成：CI `35757951098`、CD `35757951133`（均attempt 1）。
  学习步骤用宿主UID/GID运行，保持断网、只读根、cap-drop ALL和2GiB上限。
  产物 `10709291323` 存在且下载/内容校验通过，不是空上传。
- Smoke `35759427947`、固定回归 `35759427892` 成功；实际release与容器revision
  都是 `2a4f464`，容器running、重启0。运行评估仍是PASS_WITH_ACTIONS，
  保护PASS、账户同步OK、执行NOT_EVALUATED，不称为真实策略接管成功。
- Archive `35759428005`审计成功但仍INSUFFICIENT；V4 `35759427971`真实failure，
  精确核对只有原460.494秒坏段（hash/第3行/原因码匹配），120秒合同未改。
  工程裁决为ENGINEERING_PASS_DATA_NOT_QUALIFIED，不把历史数据改绿。
- 最终收据验收在原gate内执行，五组证据留在私有本地；文档仅发布脱敏摘要，
  docs-only `[skip ci]`归档，不再部署或重复跑学习。

## 收口复核与下一阶段节奏

- 工程视角：本批远端故障是Linux目录属主与capability的组合，不是学习算法失败。
  修复容器身份，不放宽目录权限或模型门槛；已在真实Linux发布链上验证。
- 验收视角：把旧run编号当作永久验收命令是执行者的流程设计错误，造成额外停顿。
  新入口保留同一失败链，核对纠正提交与必需步骤；没有任意换命令、reset或无限重试。
- 产品视角：已证明真实模型驱动的离线组合链路，仍未证明生产自学习全链路或市场优势。
  不能用本次工程修复解释/撤销旧经济研究的STOP结论。
- 节奏：本阶段到此完成，不再以发布重跑或两周观察延长。下一工程批次优先处理
  下述第1项“小时评估/六小时更新解耦”：先固定时钟、劣化回滚、冷却和更新上限的
  确定性测试，再限定改控制器并重跑同一学习链路。之后才处理生产注册/晋升链路；
  不把全部缺口混在一次发布，也不自动开启新市场研究。

## 产品目标仍未完成的部分

1. **独立小时评估未证明，而且存在实现差异。** 控制器的
   `EffectiveUpdateIntervalTicks()` 使用评估/最短更新间隔的最大值。
   本例只证明6小时更新下限，不证明每小时评估与6小时更新解耦；不得写成
   完整P0通过。这是具体后续功能缺口，不是等待样本。
2. 自适应合成收益增量包含提高仓位的影响，不是同风险归一化的市场优势。
   严格反事实 holdout 另有既有组件测试，不冒充本例已贯通该选择路径。
3. 本次调用真实训练函数，不等于生产研究调度/注册事务/真实canary已验收。
   原事务、holdout、账务、风控和回滚回归照常执行；没有合成产物生产晋升。
4. 没有合格真实市场经济候选；旧研究HALTED、旧候选关闭、旧数据失败保持。

下一决策应围绕这些具名差距，不回到期权历史补证、不取消AI/多币种目标，
不把合成正例改称盈利。新市场研究仍须新依据、可达数据与独立预算。
