# 归档资源风险：本地限定修复，远端终止根因仍待证

2026-09-22 13:00 北京时间，状态 **`LOCAL_MEMORY_REPAIR_DIAGNOSED_REMOTE_CAUSE_UNKNOWN`**。本轮“继续推进”已完成范围内可自行执行的只读诊断、限定修复和合成对照；**没有正式验收、提交、推送、部署或工作流重跑**。工程 gate 仍 BLOCKED，原失败未解决；本报告不代替根因确认 review，不申请 retry。

## 实际完成

已部署基线仍是 `a59c2b80eeb8788ba389a40d2b5fe1f9d12f8c6d`。04:57:39 UTC 只读复查五项工作流，均为原 attempt 1：CI/CD/Smoke/V4 success，Archive failure。没有把旧成功部署当成新修复的验证。

确认的源码问题是**单窗口审计长期保留所有完整快照**。已在本地增加可选快照投影：

- 旧调用默认仍返回完整快照，不改变顺序、内容和接口结果。
- Archive 调用只保留目标窗口内的目标期权对、时间戳和对冲字段；目标重复行不合并，继续触发原不合格裁决。
- 原有校验仍先作用于完整输入。窗口外/无关期权的坏数据、checksum、行数、时间倒退、段内/跨段重复冲突、交割冲突均不因投影跳过。
- 全时间范围计数、覆盖时长、输入哈希与交割证据独立保留；窗口后的有效交割证据仍可用于原裁决。
- 只有整段通过原校验后才合入全局状态；失败段不能贡献快照、计数或交割证据。

这不是截短归档、放宽门槛、重开研究或增加策略权限。元数据仍随归档身份、交割证据和段数增长，单条快照仍须完整解析；**不能声称已实现恒定内存或彻底解决所有资源问题**。

## 明确标注的合成诊断，不是门禁验收

事前限额为 30 分钟本地限定工作、诊断脚本 180 秒上限，固定已部署源码为对照；无外部行情请求、无真实归档回放、无账户访问。诊断于 **04:59:22 UTC** 完成。

13 组输入覆盖正常、窗口后交割、相同重复、目标重复行、段内/跨段时间戳冲突、段内/跨段交割冲突、窗口外坏期权行、观察边界前快照、时间倒退、行数不符、窗口外文件篡改。结果：

- 新旧默认 replay 完整返回值相同。
- 全丢弃投影、窗口投影与旧实现的全部完整性元数据相同。
- 生命周期报告除必然变化的实现身份外全字段相同，负例未变成通过。

资源对照每条快照 50 个期权行，同一固定窗口只需 3 个快照、6 个目标期权行：

| 合成快照数 | 原实现 Python 分配峰值 | 本地投影峰值 | 降幅 |
| --- | --- | --- | --- |
| 64 | 5,370,180 字节 | 1,610,649 字节 | 70.0% |
| 384 | 24,620,664 字节 | 1,673,365 字节 | 93.2% |

这是 `tracemalloc` 观测的 Python 分配，不是 ECS RSS、资源限额或 OOM 复现。只说明该合成输入上的快照驻留风险明显降低，不推断远端峰值、速度或已经修复退出 137。

另新增 **7 个单元测试方法**，保留供门禁允许后正式执行；本轮未运行 unittest/CTest/CI，不能写作“新增测试已通过”。工程/研究两个门禁前后哈希相同。

## 根因与路线复核

- **已确认：** 旧代码完整快照驻留随全归档增长；原 SSH 与 release tree 校验已通过；远端退出 137。
- **未知：** 哪个进程/内核/资源管理组件发起终止，是否为主机或 cgroup OOM，以及当时资源余量。合成内存改善不能替代这些证据。
- **目标对齐：** 降低既有只读审计的资源占用，仍以同一完整性和生命周期标准裁决；不以工程通过证明盈利方向。
- **数据可得：** 合成诊断可在本机完成；实际终止原因仍必须从 ECS 日志/平台事件取得。本机 SSH 配置及其 Include 只有非项目入口；项目 SSH 环境变量均未配置，未安装 aliyun CLI。现有工作流没有内核日志读取入口；未导出 GitHub secrets，未用输入注入或新增发布绕过门禁。
- **方法可达：** 本地对照支持修复不变性，但未取得远端根因及正式回归，当前路线仍 `unproven`，不能填写 confirmed/viable 解锁 retry。
- **预算与出口：** 本地有界工作已完成，不继续扩样本或扩大代码改造；缺主机证据即停依赖发布，不循环尝试部署。

## 唯一外部接续点与后续顺序

需要以下任一项，**不是再次请求代码/部署授权**：

1. 本机正常配置、能登录既有 ECS 的 SSH 别名；不要发送私钥、密码或交易凭据。
2. 通过 ECS 控制台提供以下时间窗内，与 OOM / killed process / SIGKILL 相关的脱敏日志及必要进程/资源信息：

```sh
journalctl -k --utc --since '2026-09-22 04:03:00 UTC' --until '2026-09-22 04:06:00 UTC' --no-pager
```

日志需有适当读取权限；不要发完整系统日志或账户资料。查不到不能据此排除 OOM，届时需补进程管理/cgroup/平台终止事件，不再盲重跑审计。

证据取得后：先确认终止来源和修复是否对应 → 原工程门禁根因/路线复盘 → 门禁允许后正式回归及一次纠正发布/原命令复验 → 精确 SHA CI/CD/post-CD 验收。若证据表明外部 kill 而非内存，则先修正实际终止来源，不把本地优化当作事故修复。再次失败立即重新阻断。

原业务结论保持：旧研究 HALTED、候选关闭、C2 NOT_QUALIFIED；本轮没有新增 Demo/live/下单/晋级资格。脱敏机器记录见[本地修复证据](2026-09-22-archive-memory-local-repair.evidence.json)，原远端失败见[部署结果及复盘](2026-09-22-runtime-source-diagnostics-result.md)。
