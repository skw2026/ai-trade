# 周频筛查交付收尾

日期：2026-09-21（Asia/Shanghai）。本次“继续推进”只完成已有筛查的工程交付，不授权第二次实验、修改风险合同或部署。

## 目标与验收

把已完成的工具、合同和结案证据整理为可提交版本，将 14 项合成测试接入 CTest。配置、构建及全量回归只运行一轮，合计上限 10 分钟；任何非预期失败按验证协议暂停。通过条件是新测试实际注册并通过、已有测试无回归、冻结身份及原始证据保持一致，交易源码、配置和工作流不变。

本次新增 `weekly_momentum_screen_test`，仅调用合成测试文件，标记 `offline;research`、超时 30 秒。不会从 CTest 调用历史筛查 CLI。原筛查实现、测试文件和机器合同不改写，保留结案报告中的三个 SHA256。

## 交付证据

**主体交付已完成：提交 `5bdfb918d502ee3b3b461c0f5ca1984050ea26cb` 已进入远端 main，未触发新的 CI/CD。** 用户在明确“提交并推送结案、跳过 CI/CD、不部署”的下一步后回复“继续推进”，本轮按该范围执行。以下本地验证及远端验收均通过 `tools/validation_gate.py run` 执行，没有非预期失败或复验，末态为 READY；这不解除业务 REJECT。

| 验收 | 实际结果 |
|---|---|
| CMake 配置 / 构建 | 均通过；原 Release build，配置及构建分别限时 120 / 180 秒 |
| 全量 CTest | 98/98 通过，167.58 秒；整轮限时 300 秒 |
| 新增测试 | CTest 实际调用 14 项合成测试，0.11 秒，30 秒测试超时及 offline/research 标签已核对 |
| 冻结身份 | 合同、筛查程序、合成测试三个 SHA256 均与原结案记录一致 |
| 原始证据完整性 | result 与归档 JSON 一致，result/source-manifest 哈希一致；48 份响应全部匹配哈希和长度，合计 2,709,759 字节 |
| 范围 | `src`、`include`、`config`、`deploy`、`.github` 相对 HEAD 无变化；未重跑筛查、未追加历史 GET、未访问账户 |

全量本地测试不是远端 CI。交付整理完成时没有新运行的 GitHub CI、远端状态核验或 ECS 直接观测；本轮远端交付仅增加 GitHub 只读核验，不能将此前主线 CI 成功套用到新增工具上，也不能从 CD 记录推断 ECS 当前进程或账户状态。

### 远端交付记录

- 推送前：2026-09-21 03:00:42 UTC，远端 main 与本地基线 `e037e24737249b7ca8ff114f35fe9a9fe8e78616` 一致；运行中/排队工作流均为 0，最新 CD 未变。
- 提交清单：精确 15 个文件，未夹带原始归档、凭据或交易配置；冻结合同、实现和测试身份未变。提交前额外 3/3 定向回归通过（4.11 秒），没有重复全量回归或历史筛查。
- 主体提交：[5bdfb91](https://github.com/skw2026/ai-trade/commit/5bdfb918d502ee3b3b461c0f5ca1984050ea26cb)，消息含 `[skip ci]`，tree 为 `ea19a84942b34c223c5f6d9410598202dd56db3b`；普通快进推送，未强推。
- 推送后：2026-09-21 03:02:30 UTC，Git 远端 main 和 GitHub commit API 均匹配完整 SHA；该 SHA 的工作流总数为 0。
- 最新 CD 仍为 [34758912163](https://github.com/skw2026/ai-trade/actions/runs/34758912163)，SHA `170d84acb8ac9547a321802f9250215c98deb0dc`，completed/success，更新时间仍为 `2026-09-13T13:19:41Z`。这是 GitHub 部署工作流记录核验，不是新的 ECS 直接观测。
- 最后只追加本报告和当前状态的交付凭证，另作 docs-only `[skip ci]` 提交；不更改已验收实现。该文档提交推送后还须核对远端 HEAD、文档外文件一致及未新增工作流/CD，最终 SHA 在本次交付回复中列明，避免循环自引用提交。

归档范围包括前序阶段检查、可行性评审、TLS 失败复盘、四份解析后的公开样例、冻结筛查合同、实现/合成测试以及筛查结果。原始筛查响应、source manifest 和独立记账诊断仍保留在本地 `.artifacts/weekly-momentum-screen-20260921/`，没有删除或替换。

注意：Git 文档中的结果和哈希不是原始 HTTP 正文备份。仅克隆仓库无法独立重算历史结果；本次不以再次请求交易所接口代替原始字节，也不扩展成数据存储系统建设。历史报告中的“未提交/推送”和旧权限字段描述各自完成时点，后续交付状态以本报告为准。

## 业务结论与下一步

- [本轮合同仍为 REJECT](2026-09-21-weekly-momentum-screen-result.md)：首次参考回撤下界 21.0296%，超过 20% 停止线。没有重新计算停止点之后的策略路径。
- 工程回归通过只代表交付质量，不改变 `NO_QUALIFIED_CANDIDATE`、C2 `NOT_QUALIFIED` 或 Demo/live 权限。
- 本阶段出口是已有结果交付，不是等两周。本轮主体交付已按上述证据验收，不运行 ECS 发布或账户验收，不以更多检查延长研究周期。
- 新的盈利研究仍需独立决定目标、相对旧失败的新依据和总预算；本次交付不自动重开 H1、降低仓位/风险门槛或启动 H2。
