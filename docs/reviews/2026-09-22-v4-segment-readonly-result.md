# V4 原件取证完成：真实超时，不靠重跑修复历史

2026-09-22 21:01 北京时间已完成单分段自动取证。诊断代码 `896911c5f7410ef087f559097e3a6d4d2f31e3b1` 已交付 main；[只读诊断 run 35730700390](https://github.com/skw2026/ai-trade/actions/runs/35730700390) attempt 1 成功，job 13 秒。**成功的是取证任务，不是 V4 数据资格或原整批工程验收。**

## 证据已从元数据推进到原件

报告 `20260922T035334.633790Z.json` 的 SHA256 与用户提供值完全一致；报告绑定的 raw/features 原件校验和、快照计数和最大耗时都匹配。共 8 条快照，第 3 条确为 460,494 ms，超过冻结 120,000 ms；不是报告统计错写，也不是校验和不匹配导致该次读取失败。

| UTC 时点 | 事实 |
| --- | --- |
| 03:56:48.404 | 第 3 次 poll 开始 |
| 04:04:26.000 | 既有内核证据记录 global OOM 杀掉 Python |
| 04:04:28.898 | 此 poll 完成，距 OOM kill 2.898 秒 |

这与同一主机资源事故窗口强烈关联，但没有请求级/调度跟踪，不能声称全部 460 秒均由 OOM 造成。**不再把缺少这种精细归因当作新的日志索取停点：原件违反冻结时效合同已经确定，足以定位当前数据缺陷。**

审计代码会拒绝该时效；采集器的 PASS 则仅说明有成功落盘的请求，未授予数据合格资格。该根因与失败 run 的 ARCHIVE_INTEGRITY_FAILURE 相符。不过历史 run 未保存拒绝段身份，且本次不是全归档重放，仍不声称已重建那次运行全部输入或证明这是其唯一拒绝原因。其他快照契约和跨段条件没有被这个诊断冒充核验。

## 阶段结论与退出条件

- **取证入口与本次定位阶段完成。** 已能由执行者调用 GitHub→ECS 只读入口，固定报告/hash/release、自动取回脱敏证据，不再要求用户逐条搬日志。
- **原工程整批仍 BLOCKED，数据仍不合格。** 过去 7 分 40 秒的观测不能通过新代码、追加正常样本或等待修复；不删除/隔离出审计输入、不缩短历史时间、不提高 120 秒阈值、不等待自然清理来获得 PASS。不提交虚假 viable 复盘或盲 retry。
- **技术与产品边界：** 可以修复未来的错误可观测性和资源隔离，但不能承诺因此让这份不可变历史输入成为合格数据。后者不是另一次部署能解决的工程缺陷。
- **本轮没有改变验收口径。** 若继续追求工程封版，建议单独冻结“部署、固定输入回归、拒绝坏输入、关闭锁存检查”的工程出口，持续数据质量另外如实 FAIL 并继续阻止研究/交易晋级。此为下一阶段的实质验收边界决策，不自动把旧全归档失败改绿。保持旧口径也是明确选择，其结果就是本批尚未通过，不存在等待两周的补救承诺。

## 自动化实际边界与安全核对

该工作流仅 `workflow_dispatch`，本次由执行者直接调用。**尚未新增后台定时器或“所有未来故障自动触发”的机制**，不承诺未配置的后台工作。只读诊断能够连续执行，不代表可以自动批准改变验收、重开研究或部署交易服务。

诊断发布使用 `[skip ci]`，其 push 隔离行为与 [GitHub 官方规则](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/skip-workflow-runs)一致；实际 API 查询该 SHA 只有上述一个诊断运行，CI/CD 为 0。最新 CD 仍为 `35711582772`、release `45a9f1b12caa3d26791ba282d7e89c4af381ea99`；远端诊断也核对了该已部署身份。未重启服务、未写 ECS 业务文件、未访问账户、未改原件、未全归档/研究复跑。

工程 gate BLOCKED、旧研究 HALTED 的文件哈希均与诊断前相同；120 秒策略、关闭候选和所有交易权限不变。本地 12 个离线诊断样例、2 条模拟 transport 路径成功，仅属于诊断检查，不替代正式 Linux/Docker/业务验收。

完整脱敏回执、原件哈希、run/artifact 身份、时间线及门禁哈希见[机器记录](2026-09-22-v4-segment-readonly-result.evidence.json)。没有复制原始快照、账户资料或主机日志到公开仓库。
