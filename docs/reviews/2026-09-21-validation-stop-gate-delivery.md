# 失败暂停与目标路径纠偏机制：阶段交付

日期：2026-09-21（Asia/Shanghai）。本项来自用户在主线工程交付期间新增的明确要求，不是重新开启研究、账户补证或交易部署。

**结论：机制已交付 main；本阶段完成，未部署 ECS。** 实现提交 `427ded0773e7690126e79cd9173742d3ccb847b5` 经独立 CI 通过后，以 `39b5dcef59db572176b71fc9fd675207dff0bf83` 合并。合并提交含 `[skip ci]`，完整文件树与独立验收分支一致，tree 为 `de1be6f858db90bc3683492e721c1528aa5789e3`。

## 后续必须怎样推进

1. 每步先固定目标、输入、通过标准、预算和失败出口；结束时明确结果、证据范围、继续/暂停决定及下一步。
2. 首次非预期失败、必需证据不足或超时，立即暂停依赖步骤，只继续只读诊断、复现和有依据的限定修复。不能直接重跑、堆样本或发布。
3. 每次都审根因和路线：目标是否对齐，数据是否真实可得，方法能否达到验收，成本与出口是否明确。未知必须保留 unknown，不用参数调整掩盖路线问题。
4. 连续两次未解决失败，或首次结构性不可达，必须重审整条路线，写明旧路线失效原因、替代方案依据、有限预算与停止条件；涉及目标、验收或权限变化，交由用户决定。
5. 有证据的复盘只授权一次同工作目录、同命令复验；通过才恢复推进，再失败就重新阻断。判断应该停止时可以 HALTED，不要求把结果修成绿色。

根目录 `AGENTS.md` 固定入口规则，[验证协议](../VALIDATION_PROTOCOL.md) 定义复盘字段。`tools/validation_gate.py` 持久保存失败与复盘哈希，拒绝无复盘重试、弱化命令、换目录、复盘授权后篡改和并发绕过；没有 force/reset 入口。CI 的 configure/build/test 已接入同一门禁。

## 验证与交付步骤

- 首轮全量本地构建及 CTest：97/97，通过时间 170.61 秒。随后静态检查补强命令身份的工作目录绑定；该补强后的门禁测试为 11/11，注册回归为 3/3（4.27 秒）。不把此前全量运行冒称补强后重跑。
- 最终版本 [独立 CI 35524397272](https://github.com/skw2026/ai-trade/actions/runs/35524397272) 在精确提交 `427ded0` 上完整通过；job `106113926388`，Test 步骤于 `2026-09-20T17:02:52Z` 成功。期间没有非预期失败或无诊断重试。
- YAML 语法、diff 检查通过；交易源码、配置和部署目录相对前一主线没有变化。负例测试中的预期失败由测试断言检查，不是被忽略的项目失败。
- 合并树一致性检查通过，远端 main 指向 `39b5dce` 已核验。精确合并 SHA 的工作流查询为 0 项；最新 CD 仍为 `34758912163` / `170d84acb8ac9547a321802f9250215c98deb0dc`，更新时间仍是 9 月 13 日。合并提交没有新跑 CI，依据是分支独立 CI 和一致的完整文件树。
- 最后仅追加本报告与当前状态链接，使用 docs-only `[skip ci]` 归档，不修改已通过验证的实现。

## 限制与下一步

门禁能校验流程和复验条件，不能证明根因判断真实；退出码 0 不自动代表业务验收通过。状态不跨机器自动同步，继续工作前仍须审查分支 CI 和阶段报告。重要失败与复盘须归档，不得删除状态或换路径规避。

本次没有 ECS 直接新快照、服务重启、下单、资金操作或研究重开。账户完整证据不足、`NO_QUALIFIED_CANDIDATE`、C2 `NOT_QUALIFIED` 保持原结论。工程及机制交付不等于策略盈利资格。

本阶段后没有必须再等两周的动作。后续在已批准目标内一律按上述机制推进；若进入新的策略阶段，先做单一假设、最多一个有效工程日的可行性立项评审，核定数据、完整成本、周期与失败出口，只决定立项/不立项。该新阶段尚未启动，不自动部署或延续旧研究。

机器证据见[交付 JSON](2026-09-21-validation-stop-gate-delivery.evidence.json)；此前[工程主线交付](2026-09-21-mainline-engineering-delivery.md)独立保留。
