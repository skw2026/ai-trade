# 自进化评估/更新时钟解耦结果

2026-09-23，本批按[事前合同](../plans/2026-09-23-evolution-clocks.md)自动推进。
最终结案：**EVOLUTION_CLOCK_DECOUPLING_DELIVERED**。
代码`71783fae45e83d3ef615bcb073061470c52bc9e1`已推送main并真实部署。
本地/CI/Docker均111/111，远端六组真实学习验收及部署后精确收据通过。
这是时钟能力交付，不是整个生产自学习或盈利资格通过。
详见[脱敏机器证据](2026-09-23-evolution-clocks.evidence.json)。
沿用已READY的离线工程gate历史；没有删除失败、重置HALTED或启动新市场研究。

## 具体修复

原控制器以`max(update_interval_ticks, min_update_interval_ticks)`安排评估，
因此六小时调权下限同时阻止了小时评估。这是代码语义耦合，不是缺历史数据，
也不能靠等待两周解决。

现在评估周期独立；普通实际调权受全局最短间隔控制，首次启动同样等待，
切换币种/市场状态分桶不绕开限频，无变化或证据拒绝不消耗成功调权间隔。
符合原有条件的退化回滚按评估节奏检查，不再被普通更新间隔拖延；
冷却内继续评估但禁止调权。strict temporal-holdout原有回滚政策保持。

新增`clock_tick_interval_ms`：0兼容原消息计数，正数明确一个逻辑tick的毫秒数。
固定测试300000ms，12/72/288分别为1h/6h/24h；应用传入行情事件时间，日志
报告逻辑时钟、下一次更新及无效时间计数。相同时间消息不加速，缺失/倒退
时间拒绝，大跳跃只评估一次真实窗口、不补造空窗口。

## 验证证据

- 本地完整CTest111/111（177.39秒），验收驱动单测11/11；新增原生时钟测试
  在macOS和实际Linux研究运行环境均通过。
- 新原生回归覆盖启动、普通限频、跨分桶、无变化、回滚、冷却、时间突发/
  倒退/跳跃，以及learnability/严格holdout证据不足仍拒绝。
- 应用接线回归确认市场消息计数不会代替事件时钟，状态指纹区分时钟模式。
- Linux断网/只读根/无capabilities/2GiB实际模型验收六组通过，未改合成数据、
  模型配置、费用或经济判据。Python独立按原始时间戳复核每次评估与调权。
- 正常自适应场景85次小时评估、7次实际调权；反转128次小时评估、7次实际
  调权、3次回滚、58次冷却评估。所有普通调权至少间隔6小时，回滚后24小时
  不调权。动作次数与旧耦合控制器不同，不以旧次数为正确性标准。
- 原正例495个完整episode、反转742个；噪声/打乱标签/高成本均拒绝；账务
  独立重建、Python/C++推理一致及strict接管拒绝合成证据均保持。

私有产物位于`.artifacts/evolution-clocks-20260923/`，不公开原始账户资料。

## 发布与收口复核

- CI [35803490079](https://github.com/skw2026/ai-trade/actions/runs/35803490079)：
  111/111、132.92秒；CD [35803490109](https://github.com/skw2026/ai-trade/actions/runs/35803490109)：
  Docker内111/111、124.91秒，真实学习步骤、证据上传和部署均success，全部attempt1。
- Smoke35804297640、固定工程35804297654成功；实际release/容器revision均
  为71783fa，running、重启0。运行PASS_WITH_ACTIONS，保护PASS、账户同步OK，
  execution仍NOT_EVALUATED，不冒称真实策略接管。
- Archive35804297681仍INSUFFICIENT；V4 35804297680真实failure，原件hash、
  第3行、POLL_LATENCY_EXCEEDS_CONTRACT均匹配唯一已知460.494秒坏段。
  数据质量仍FAIL，120秒标准未改，没有新增坏段被忽略。
- 五组产物及测试日志已下载；报告、学习产物、源码hash与本次SHA匹配。
  原gate最终READY，旧研究/旧工程HALTED哈希保持。
- 唯一本批失败是本地GitHub状态查询的TLS握手超时。先暂停、确认远端同SHA
  已成功及查询恢复，再按[复盘](2026-09-23-clock-observer-transport-review.json)
  做一次原命令复验；没有rerun、纠正发版或换SHA。更底层网络原因unknown，
  不猜测；原失败保留。

工程视角：原max耦合已移除，新增全局更新下限与显式时间单位，测试直接覆盖
高频消息和跨桶绕过风险。验收视角：先冻结标准，正常、负例和回滚共用实际
模型链，部署与数据资格分开且不删坏段。产品视角：本批填补一个可确认的实现
缺口，不把小时评估次数、合成盈利或CI通过放大成完整产品验收。

## 边界与下一节奏

本批交付可配置、应用已接线的时钟能力及离线证据；现有线上策略配置没有改，
不能声称线上已经每小时评估，也不是无行情时自动唤醒的墙钟任务。
评估变密不会自动增加有效统计样本：启用时间模式前仍须核对所选profile的
样本数、holdout与learnability可达性，不降低这些门槛换取更新。

工程交付完成后，下一优先项是生产注册/晋升链的隔离证据贯通：先明确真实
模型产物与现有事务测试之间的缺口，证明拒绝/回滚边界；不得把TEST_ONLY
报告改名为市场资格，也不为合成正例写入生产注册表或启用真实canary。
本次只读盘点确认`test_model_registry.py`和`test_evaluate_activation_transaction.py`
尚未列入CTest/CI必需测试；现有111/111仅指已注册集合，不冒称覆盖全部Python
测试。下一批先将这两组纳入强制回归，再用本次真实训练产物验证隔离注册目录
中的拒绝与身份一致性，并补足pending/rollback证据。若缺真实市场资格，结果
仍必须拒绝晋升，不能增加合成报告豁免。普通工程步骤沿用持续授权，不逐步索权；
仍需按原gate处理任何失败，不能在收口记录后暗中重跑或恢复研究。
这不需要两周等待；需要的是有输入身份和失败出口的可执行验收。
市场盈利、完整生产自学习和Demo策略接管资格仍未证明，旧关闭研究不重开。
