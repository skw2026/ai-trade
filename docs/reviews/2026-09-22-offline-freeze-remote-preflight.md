# 封版后远端预检：通过，公开推送被权限边界阻断

## 当前结果

**`REMOTE_PREFLIGHT_PASS_PUBLIC_PUSH_APPROVAL_REQUIRED`。本地封版保持完成，远端 CI 和异地备份没有完成。** 这是需要用户决定的公开发布边界，不是需要再跑几天的策略验证。

用户要求继续后，按[固定范围](../plans/2026-09-22-offline-freeze-remote-acceptance.md)检查现有仓库、工作流及备份入口。17:15:35 UTC 开始（北京时间 9 月 22 日 01:15:35），预检通过后尝试申请推送权限；平台在命令执行前拒绝，未执行 Git push，没有改用其他工具或账号绕过。

## 已确认的事实

| 对象 | 新观察结果 |
|---|---|
| 本地封版 | `a1079a5772389c9059128293e82bfdd23863999d`；代码/配置未修改，原本地 105/105 工程回归不冒称新远端测试 |
| 仓库 | GitHub API 确认为公开 `skw2026/ai-trade`，default branch 为 main |
| Git SSH | 可只读查询远端；main 仍为 `9b76b3daffc433bfc1a4f2f764415d51a24fa98d` |
| 独立验证分支 | `validation/offline-freeze-20260922` 原不存在，拒绝推送后再次查询仍不存在 |
| 工作流边界 | 12 份工作流与冻结 Git blobs 逐字节一致；拟定分支的 push 只匹配 CI，不匹配 main CD、研究 tag 或旧具名分支 |
| 最新 CD | `34758912163` / `170d84acb8ac9547a321802f9250215c98deb0dc`，success，updated_at 仍为 `2026-09-13T13:19:41Z`；只是 GitHub 记录，不是新 ECS 观测 |
| 旧研究 gate | 仍 HALTED，SHA256 `bc576299059d630fcf400de46dc16b38d6c83591c1dc193433a9c32322f6910b` 未变 |
| 本机归档 | 再次流式校验 1,000 文件、14,098,060 字节成功；SHA256 `d2626cfdf85670cdd0ca294b6ef45f520b3dfe4b49443944c23e8d6fde4caad1`；未解包执行、未上传、未删原件 |

机器观察与身份见[同名证据](2026-09-22-offline-freeze-remote-preflight.evidence.json)。完整公开 API 响应仅在 `.artifacts/engineering-remote-20260922/` 保存。本轮未查询交易账户、主机凭据或账户原文。

## 连接与失败处理

本机 GitHub CLI 未登录，配置的 Git HTTPS 凭据助手也没有可用非交互凭据。按插件连接检查流程找到了未连接的 GitHub 入口；随后确认现有 SSH + 公开 API 已足够完成只读验收，因此**插件连接不是当前阻断**。Python 默认 TLS 证书链检查失败，系统 curl 使用正常 TLS 验证成功；没有关闭证书校验或访问其他凭据来源。

正式预检首次因新临时脚本导入未安装的 PyYAML 失败。依赖步骤立即暂停，推送未开始；[复盘](2026-09-22-remote-freeze-preflight-review.json)确认是检查器引入未声明依赖。修复仅移除该依赖，改为已逐项审阅的完整冻结工作流清单及全字节身份保护；不修改工作流、分支、验收阈值或研究 gate。同 cwd、同 argv 原命令一次复验成功。首轮错误和门禁记录保留，后续归档检查也通过。

计数勘误：复盘中的 `six legacy branch routes` 为文字计数错误；实际是 5 份旧分支限定工作流列出 8 个具名分支，另有 main、research tag 和 CD completion 入口。保留已绑定复盘的原始字节，以预检记录的 12 份完整文件及实际触发条件为准；拟定验证分支不匹配这些旧入口。

## 真正的人工决策点

计划把整套已审查封版代码推到现有公开仓库的新验证分支。虽然不是交易部署，但会使新增实现与必要验收文档公开可见。平台未将上一条“继续”视为对该公开 payload 的明确授权，推送被拒绝；这与已通过的技术预检是两件事。

需要确认的具体范围：**允许公开推送封版代码和必要验收文档到 `skw2026/ai-trade` 的独立验证分支，运行一次现有 CI；不更新 main、不推送 research 标签、不部署、不上传原始数据或证据归档。** 确认后继续原有计划，不再为普通步骤逐次索权；若 CI 失败则按协议停下诊断。

异地备份仍需私有存储目的地。未收到具体位置前保留本机归档，不擅自建公开 Release、公开数据分支或把旧历史原文纳入 Git。无须购买数据、等待两周或重开任何关闭策略。
