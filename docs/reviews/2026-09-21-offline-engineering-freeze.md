# 离线工程封版：验收、证据与结案

## 结论

**`OFFLINE_ENGINEERING_FREEZE_COMPLETE`，随包含本报告的本地 Git 提交生效。** 交付范围为已实现的离线 MVP 工程、回归、证据保存与使用说明；不是盈利、历史账户资格、Demo 激活或交易 release。

用户选择“先完成工程封版”，依据[事前范围](../plans/2026-09-21-offline-engineering-freeze.md)连续完成。2026-09-21 15:50:43 UTC 开始，16:04:08 UTC 全量回归通过，随后仅编译诊断目标、封存和交付检查；当地日期跨至 9 月 22 日。未超过主动设置的 2h 上限。代码基线为 `9b76b3daffc433bfc1a4f2f764415d51a24fa98d`；精确交付 SHA 由包含本报告的提交和本地提交后回执绑定，避免报告引用自身提交哈希的循环。

## 本次交付与验收

| 项目 | 实际结果与范围 |
|---|---|
| 现有离线实现审查 | 闭合 5m 信号、下一 open 成交、人工风险周期、参考资本与输入/裁决链路纳入本地交付；本次未改这些冻结生产实现的金融含义、策略参数或参考合同 |
| 干净构建 | 全新 `.artifacts/engineering-freeze-20260921/build`，Debug；保留旧 `build/` 及其研究绑定二进制 |
| 测试产物身份 | pipeline 测试必须接收当前 CMake 产物 `--binary`，组合测试核对 plan/receipt 中的实际二进制身份；不再静默调用旧 `build/trade_bot` |
| 新增/接入回归 | 采集器 6 项、停止账务 8 项、归档 7 项合成测试接入 CTest；pipeline 29 项及失败门禁 14 项保留 |
| 定向回归 | 9/9 PASS，13.73 秒；后新增的归档测试独立通过，并进入最终全量回归 |
| 全量回归 | **105/105 PASS，169.37 秒**；没有排除失败测试、降低断言或复跑历史策略 |
| 诊断工具可编译 | 两个 `EXCLUDE_FROM_ALL` 审计目标在新目录编译成功；本阶段未执行其历史诊断入口 |
| 原证据完整性 | **999 项**旧源码/合同/配置/输入/日志/二进制及原研究 gate 身份与既有记录一致 |
| 归档完整性 | 生成 1,000 个负载文件的独占创建归档；归档后重新打开并逐成员验证 SHA256、大小与路径，全程不解包执行 |
| 交付边界 | 本地代码提交使用 `[skip ci]`；不推送、不查询或改变远端运行状态，不部署、不重启、不访问账户 |

环境：Darwin arm64、AppleClang 17、CMake 4.2.1、Python 3.14.7，CatBoost/Beast 默认关闭。上述结果不是本次 Linux/Docker/远端 CI 的验收；此前远端成功记录不能冒充此新提交的 CI。

## 唯一一次失败与纠偏

首次全量 CTest 在第 4 项 `trade_system_test` 停止，退出码 8。最早失败点是测试只从 `cwd/config` 和 `cwd/../config` 找配置，不能适配本次隔离的嵌套构建目录；所需 YAML 一直存在。这不是风险阈值不通过，也不是生产配置加载缺陷。

立即暂停归档/提交，保存首轮失败日志；[根因及路线复核](2026-09-21-engineering-freeze-test-path-review.json)获门禁接受后，仅修复测试入口：CMake 显式传入源码配置目录，保留原 11 个配置文件和全部断言。一次受审核的重建成功后仍未放行依赖步骤，直到**同 cwd、同 argv 的原全量命令**唯一一次复验通过。未清空状态、重建另一 gate 或换成更弱验收。

正式原命令、复盘身份、失败/修复/成功时间及测试文件身份保存在[机器证据](2026-09-21-offline-engineering-freeze.evidence.json)。日志仍在本地：

- `.artifacts/engineering-freeze-20260921/first-full-ctest-failure.log`
- `.artifacts/engineering-freeze-20260921/full-ctest.log`
- `.artifacts/engineering-freeze-20260921/targeted-ctest.log`
- `.artifacts/engineering-freeze-20260921/validation-state.json`

工程 gate 最终允许工程检查继续；**原研究 gate 仍为 HALTED**，SHA256 `bc576299059d630fcf400de46dc16b38d6c83591c1dc193433a9c32322f6910b`。两个状态对应不同的用户授权阶段，不能互相替代。

## 数据封存与恢复边界

归档：`.artifacts/engineering-freeze-20260921/closed-mvp-evidence.tar.gz`

- SHA256：`d2626cfdf85670cdd0ca294b6ef45f520b3dfe4b49443944c23e8d6fde4caad1`
- 压缩大小：14,098,060 字节；负载原始大小：92,540,813 字节。
- 负载文件数：1,000（另有内嵌 manifest）；既有身份核对数：999。
- 负载清单 SHA256：`88f1a59b2591daaee027b7b754ac0e30cc58d64b7a729940e79ae4b6fcbf2df0`。

公开原响应、限流失败、manifest、CSV、已关闭唯一 base 的日志/结果、合成工程失败记录、现存绑定旧二进制和核心源码均已封存。未打包真实账户、凭据或 `.env`，原件没有删除。新工程测试日志另留于上列目录，哈希随本报告的机器证据提交，不冒称它们位于历史归档内部。

这是**同机副本，不是异地灾备**；Git 提交不会包含原始数据或归档。完整检查与全新临时目录恢复方法见[使用说明](../OFFLINE_ENGINEERING_FREEZE.md)。归档流式读取/逐文件校验已通过，本轮未实际解包或执行；旧绝对路径/平台二进制不是可移植续跑检查点，也不能恢复此前已覆盖的所有中间版本。

## 多视角复盘与收口

以下为同一执行者按不同职责核查，不冒充独立外审或多人会议。

| 视角 | 本次结论与具体处置 |
|---|---|
| 产品/项目 | 封版目标固定为可定位的代码、通过的工程测试、可校验的证据；不把“盈利尚未通过”转成无限工程任务。到本地提交即结案，不安排默认两周等待 |
| 质量/工程 | 脆弱点是测试隐式依赖旧构建/目录，而不是增加运行时间能解决；显式绑定当前二进制和配置路径，干净构建通过后封存，不用旧测试快照顶替 |
| 研究/风险 | 999 项旧身份未变，原 MVP 风控边界触发、三路线不立项、ETF 时点不足和 C2 未通过照旧；工程 PASS 不改变经济结论，不重开 base/stress 或风险周期 |
| 运维/证据 | 本机归档已完整校验，但没有异地备份和新提交远端 CI；这些限制必须写明，不将本地封版包装成生产可用 release |

## 阶段终点与下一步

**工程封版阶段完成，不再要求用户批准本阶段内部步骤。** 当前无需要等待市场时间才能通过的工程项，也无继续旧策略实验的授权。保持现状，不自动训练、重开研究或激活 Demo。

若以后安排外部交付，应单独确定代码远端目的地/只读 CI 范围和历史归档的异地存储位置，再执行推送或上传；两者都不默认包含交易部署。新的盈利研究必须有与关闭路线实质不同的机制、可得证据和有限否定出口，不能从工程通过直接跳到两周观察。
