# 主线工程交付：合入 main，不部署 ECS

日期：2026-09-21（Asia/Shanghai）。本次用户批准的阶段目标是将已验证的修复正式交付主线，并逐步确认结果；不包含 ECS 发布、账户操作或新策略研究。

**阶段结论：工程修复已正式进入 main，未部署 ECS；本阶段完成，不安排两周等待。** 这不是完整账户认证、生产回滚验收或盈利策略交付。

## 步骤与结果

1. **合并前核对完成。** 远端 main 为 `833a6d90644e0bd88545dafe97c408beb9c3a67d`，没有新增主线改动；工作区干净。验收分支 `8b002bf` 相对独立 CI 成功的 `421475b` 仅新增文档，全部非 docs 文件一致。重新读取 [CI 35523224877](https://github.com/skw2026/ai-trade/actions/runs/35523224877)，确认精确 SHA 成功。随后执行无部署合并。
2. **主线合并、验证、推送完成。** 使用保留双亲历史的非快进合并，得到 `5b361969f44f55c88cd479af78634fe1010a2783`，提交信息带 `[skip ci]`。合并文件树与验收分支逐字节一致，Git tree 均为 `4fb9249be64e4b84cd4b40988ed07f60dc1024bd`；7/7 定向测试通过（2.05 秒），覆盖交易核心、时序、回放比较、收口约束、只读账户、发布门禁和 release 完整性。远端 main 已确认指向该合并提交。随后检查自动化与部署边界。
3. **无部署边界核验完成。** GitHub 对精确合并 SHA 的工作流查询返回 0 项；最新 CD 前后均为 9 月 13 日的 `34758912163` / release `170d84a`，没有新的交易 CD。最终只追加本交付文档和当前状态说明，以 docs-only `[skip ci]` 推送；不改已验证的源码、配置或工作流。

合并提交本身没有新跑 GitHub CI，这是有意跳过自动 CI/CD。验证依据是已成功的精确分支 CI、合并树身份一致及合并后的定向回归，不把旧 CI 改标为 main 合并提交的 CI。

## ECS 与恢复证据范围

- 本轮没有执行部署、服务重启、下单、账户模式调整或资金划转。
- 最近一次直接 ECS 身份快照来自[此前只读验收](2026-09-21-engineering-remote-acceptance.md)，时刻为 `2026-09-20T16:36:17.549002Z`，release 为 `170d84acb8ac9547a321802f9250215c98deb0dc`。
- 本次合并后的检查为 GitHub 主线与 CD 记录核对，没有再次登录 ECS 取得新快照；不把前次快照称为合并后的直接观测。
- 发布/恢复的隔离回归证据保留，未执行真实环境回滚。主线更新与 ECS 运行版本不同是本次明确选择，不是漏发。

## 最终交付与后续计划

已交付：离线回放确定性修复、确认/成交先后及去重恢复回归、完整选定轨迹比较器、固定旧账户归档只读复核入口、机器证据和可复现说明。原始失败记录及验收分支保留，不删除数据、不重写历史。

仍有边界：完整账户证据不足；生产回滚未演练；研究 `NO_QUALIFIED_CANDIDATE`；C2 `NOT_QUALIFIED`。这些不因代码合并转成通过，也不自动扩成补证任务。

**本阶段之后不自动开展新工程修复、交易部署或策略实验。** 下一阶段需要独立方向决定：若继续以策略为目标，建议先做最多一个有效工程日、仅一个新假设的可行性立项评审，先核定数据、完整成本、样本周期和失败出口，只输出“立项 / 不立项”；它尚未启动。现有安全监控与采集保持原有配置，不为此安排新的等待周期。

证据见[主线交付 JSON](2026-09-21-mainline-engineering-delivery.evidence.json)；前序[本地修复](2026-09-20-engineering-defect-closeout.md)和[独立验收](2026-09-21-engineering-remote-acceptance.md)继续保留各自时点的事实。
